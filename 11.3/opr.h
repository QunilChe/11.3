#pragma once
#include "opr.h"
#include "var.h"
using namespace nsVar;
namespace nsOpr {
	int BinSearch(Student* p);
	double LineSearch(Student* p);
	double LineSearch1(Student* p);
	double LineSearch2(Student* p);
	double Line(Student* p);
	double Line1(Student* p);
	void Sort(Student* p);
	double Line2(Student* p);
	int* IndexSort(Student* p, char* fname);
}